============
Installation
============

At the command line::

    $ easy_install django-safari-notifications

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-safari-notifications
    $ pip install django-safari-notifications
